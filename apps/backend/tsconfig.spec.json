{
  "extends": "./tsconfig.json",      // notice: extend local app tsconfig, not base
  "compilerOptions": {
    "types": ["node", "jest"],
    "noEmit": true                   // don’t emit test files
  },
  "include": ["src/**/*.spec.ts", "src/**/*.test.ts", "src/**/*.ts"],
  "exclude": [],
  "references": [    
    { "path": "../../libs/shared" }
  ]
}