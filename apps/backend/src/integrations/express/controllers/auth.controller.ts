import { Request, Response } from 'express';


export const register = async (
  req: Request,
  res: Response
) => {
  // await registerUser();
};

export const login = async (
  req: Request,
  res: Response
) => {
  
};
