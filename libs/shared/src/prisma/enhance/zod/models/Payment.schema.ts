/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
import { PaymentMethodSchema } from '../enums/PaymentMethod.schema';
import { PaymentStatusSchema } from '../enums/PaymentStatus.schema';
const baseSchema = z.object({
    id: z.string(),
    amount: z.number(),
    method: PaymentMethodSchema,
    status: PaymentStatusSchema,
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
}
).strict();
const relationSchema = z.object({
    order: z.record(z.unknown()),
}
);
const fkSchema = z.object({
    orderId: z.string(),
}
);

/**
 * `Payment` schema excluding foreign keys and relations.
 */
export const PaymentScalarSchema = baseSchema;


/**
 * `Payment` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const PaymentSchema = PaymentScalarSchema.merge(fkSchema).merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const PaymentPrismaCreateSchema = baseSchema.partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const PaymentPrismaUpdateSchema = z.object({
    id: z.string(),
    amount: z.union([z.number(), z.record(z.unknown())]),
    method: PaymentMethodSchema,
    status: PaymentStatusSchema,
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date()
}).partial().passthrough();


/**
 * `Payment` schema for create operations excluding foreign keys and relations.
 */
export const PaymentCreateScalarSchema = baseSchema.partial({
    id: true, createdAt: true, updatedAt: true
});


/**
 * `Payment` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const PaymentCreateSchema = PaymentCreateScalarSchema.merge(fkSchema);


/**
 * `Payment` schema for update operations excluding foreign keys and relations.
 */
export const PaymentUpdateScalarSchema = baseSchema.partial();


/**
 * `Payment` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const PaymentUpdateSchema = PaymentUpdateScalarSchema.merge(fkSchema.partial());

