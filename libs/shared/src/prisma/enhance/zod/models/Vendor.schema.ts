/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
import { VendorStatus } from '../../models';
import { VendorStatusSchema } from '../enums/VendorStatus.schema';
const baseSchema = z.object({
    id: z.string(),
    name: z.string(),
    slug: z.string(),
    description: z.string().nullish(),
    status: VendorStatusSchema,
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
}
).strict();
const relationSchema = z.object({
    users: z.array(z.unknown()).optional(),
    products: z.array(z.unknown()).optional(),
}
);

/**
 * `Vendor` schema excluding foreign keys and relations.
 */
export const VendorScalarSchema = baseSchema;


/**
 * `Vendor` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const VendorSchema = VendorScalarSchema.merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const VendorPrismaCreateSchema = baseSchema.partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const VendorPrismaUpdateSchema = z.object({
    id: z.string(),
    name: z.string(),
    slug: z.string(),
    description: z.string().nullish(),
    status: VendorStatusSchema,
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date()
}).partial().passthrough();


/**
 * `Vendor` schema for create operations excluding foreign keys and relations.
 */
export const VendorCreateScalarSchema = baseSchema.partial({
    id: true, status: true, createdAt: true, updatedAt: true
});


/**
 * `Vendor` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const VendorCreateSchema = baseSchema.partial({
    id: true, status: true, createdAt: true, updatedAt: true
});


/**
 * `Vendor` schema for update operations excluding foreign keys and relations.
 */
export const VendorUpdateScalarSchema = baseSchema.partial();


/**
 * `Vendor` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const VendorUpdateSchema = VendorUpdateScalarSchema;

