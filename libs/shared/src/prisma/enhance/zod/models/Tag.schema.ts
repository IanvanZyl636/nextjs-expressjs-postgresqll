/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
const baseSchema = z.object({
    id: z.string(),
    name: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
}
).strict();
const relationSchema = z.object({
    products: z.array(z.unknown()).optional(),
}
);

/**
 * `Tag` schema excluding foreign keys and relations.
 */
export const TagScalarSchema = baseSchema;


/**
 * `Tag` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const TagSchema = TagScalarSchema.merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const TagPrismaCreateSchema = baseSchema.partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const TagPrismaUpdateSchema = z.object({
    id: z.string(),
    name: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date()
}).partial().passthrough();


/**
 * `Tag` schema for create operations excluding foreign keys and relations.
 */
export const TagCreateScalarSchema = baseSchema.partial({
    id: true, createdAt: true, updatedAt: true
});


/**
 * `Tag` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const TagCreateSchema = baseSchema.partial({
    id: true, createdAt: true, updatedAt: true
});


/**
 * `Tag` schema for update operations excluding foreign keys and relations.
 */
export const TagUpdateScalarSchema = baseSchema.partial();


/**
 * `Tag` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const TagUpdateSchema = TagUpdateScalarSchema;

