/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
import { DimensionsSchema } from './Dimensions.schema';
const baseSchema = z.object({
    id: z.string(),
    featured: z.boolean().default(false),
    sku: z.string().nullish(),
    slug: z.string(),
    name: z.string(),
    description: z.string().nullish(),
    color: z.string().nullish(),
    weight: z.number().nullish(),
    dimensions: z.lazy(() => DimensionsSchema).nullish(),
    price: z.number(),
    stock: z.number(),
    salePrice: z.number().nullish(),
    saleStart: z.coerce.date().nullish(),
    saleEnd: z.coerce.date().nullish(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    deletedAt: z.coerce.date().nullish(),
}
).strict();
const relationSchema = z.object({
    product: z.record(z.unknown()),
    orderItems: z.array(z.unknown()).optional(),
    cartItems: z.array(z.unknown()).optional(),
    mediaItems: z.array(z.unknown()).optional(),
}
);
const fkSchema = z.object({
    productId: z.string(),
}
);

/**
 * `ProductVariant` schema excluding foreign keys and relations.
 */
export const ProductVariantScalarSchema = baseSchema;


/**
 * `ProductVariant` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const ProductVariantSchema = ProductVariantScalarSchema.merge(fkSchema).merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const ProductVariantPrismaCreateSchema = baseSchema.partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const ProductVariantPrismaUpdateSchema = z.object({
    id: z.string(),
    featured: z.boolean().default(false),
    sku: z.string().nullish(),
    slug: z.string(),
    name: z.string(),
    description: z.string().nullish(),
    color: z.string().nullish(),
    weight: z.union([z.number().nullish(), z.record(z.unknown())]),
    dimensions: z.lazy(() => DimensionsSchema).nullish(),
    price: z.union([z.number(), z.record(z.unknown())]),
    stock: z.union([z.number(), z.record(z.unknown())]),
    salePrice: z.union([z.number().nullish(), z.record(z.unknown())]),
    saleStart: z.coerce.date().nullish(),
    saleEnd: z.coerce.date().nullish(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    deletedAt: z.coerce.date().nullish()
}).partial().passthrough();


/**
 * `ProductVariant` schema for create operations excluding foreign keys and relations.
 */
export const ProductVariantCreateScalarSchema = baseSchema.partial({
    id: true, featured: true, createdAt: true, updatedAt: true
});


/**
 * `ProductVariant` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const ProductVariantCreateSchema = ProductVariantCreateScalarSchema.merge(fkSchema);


/**
 * `ProductVariant` schema for update operations excluding foreign keys and relations.
 */
export const ProductVariantUpdateScalarSchema = baseSchema.partial();


/**
 * `ProductVariant` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const ProductVariantUpdateSchema = ProductVariantUpdateScalarSchema.merge(fkSchema.partial());

