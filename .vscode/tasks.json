{
	"version": "2.0.0",
	"tasks": [		
		{
			"label": "build",
			"type": "shell",
			"command": "npm run build"
		},
		{
			"label": "prisma:reset:force",
			"type": "shell",
			"command": "npm run prisma:reset:force"
		},
		{
			"label": "prelaunch-all",
			"dependsOn": ["build", "prisma:reset:force"],
			"dependsOrder": "parallel"
		}
	]
}